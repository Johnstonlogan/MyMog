interface Values {
  backgroundColor: string;
  borderRadius: number;
  border: boolean;
}

export const StyledLayoutClass: any = (values: Values) => {
  let layout: Array<string> = [];

  return layout;
};
